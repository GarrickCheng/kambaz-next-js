// Simplified web vitals reporting - compatible with current web-vitals version
const reportWebVitals = (onPerfEntry?: (metric: unknown) => void) => {
  if (onPerfEntry && typeof onPerfEntry === 'function') {
    // Web vitals reporting can be implemented here if needed
    // For now, this is a placeholder to prevent build errors
    console.log('Web vitals reporting placeholder');
  }
};

export default reportWebVitals;
